include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"

linter:
  rules:
    # Use case builders are demo entry points discovered by codegen, not a
    # public API surface, so documentation on each is just noise.
    public_member_api_docs: false
